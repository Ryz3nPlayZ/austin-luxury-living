# Copy to .env.local (do NOT commit secrets to git)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Optional: service role key (server only, never expose to client)
# VITE_SUPABASE_SERVICE_ROLE_KEY=your-service-role-key (do NOT add to client-side env)
